name: first

on: push

jobs:
  job1:
    name: First job
    runs-on: ubuntu-latest

    steps:
      - name: step one
        uses: actions/checkout@v2
      - name: run system test1
        uses: ./.github/workflows/run_module_system_tests.yml
        with:
          module_name: nidigital
